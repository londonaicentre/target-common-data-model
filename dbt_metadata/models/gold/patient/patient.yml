# AUTO-GENERATED by the target-common-data-model (CDM) spec repo - DO NOT EDIT.
# Source of truth: cdm/Patient.yaml in that repo. Regenerate there; edits here are overwritten.
models:
- name: patient
  description: Patient dimension. One row per Patient resource, keyed by master_person_id. Referenced
    by clinical fact tables. Analogue of OMOP person.
  columns:
  - name: master_person_id
    data_type: varchar
    description: Master person id for this Patient resource. One row per patient.
    data_tests:
    - unique
    - not_null
    config:
      meta:
        fhir_path: Patient.id
        fhir_type: id
  - name: nhs_number
    data_type: varchar
    description: NHS number (10-digit national patient identifier).
    config:
      meta:
        fhir_path: Patient.identifier:nhsNumber.value
        fhir_type: string
  - name: active
    data_type: boolean
    description: Whether this patient record is in active use.
    config:
      meta:
        fhir_path: Patient.active
        fhir_type: boolean
  - name: gender
    data_type: varchar
    description: Administrative gender used for record-keeping.
    data_tests:
    - accepted_values:
        arguments:
          values:
          - male
          - female
          - other
          - unknown
    config:
      meta:
        fhir_path: Patient.gender
        fhir_type: code
        value_set: http://hl7.org/fhir/ValueSet/administrative-gender|4.0.1
  - name: birth_date
    data_type: date
    description: Date of birth.
    config:
      meta:
        fhir_path: Patient.birthDate
        fhir_type: date
  - name: deceased_boolean
    data_type: boolean
    description: Whether the patient is recorded as deceased.
    config:
      meta:
        fhir_path: Patient.deceasedBoolean
        fhir_type: boolean
  - name: deceased_datetime
    data_type: timestamp_ntz
    description: Date/time of death, when known.
    config:
      meta:
        fhir_path: Patient.deceasedDateTime
        fhir_type: dateTime
  - name: ethnic_category
    data_type: varchar
    description: Ethnic category code (NHS England ethnic category).
    data_tests:
    - accepted_values:
        arguments:
          values:
          - A
          - B
          - C
          - D
          - E
          - F
          - G
          - H
          - J
          - K
          - L
          - M
          - N
          - P
          - R
          - S
          - Z
          - '99'
    config:
      meta:
        fhir_path: Patient.extension:ethnicCategory
        fhir_type: code
        value_set: https://fhir.hl7.org.uk/CodeSystem/UKCore-EthnicCategoryEngland
  - name: ethnic_category_display
    data_type: varchar
    description: Human-readable label for ethnic_category. Not a FHIR element.
  - name: marital_status
    data_type: varchar
    description: Marital (civil) status code (NHS England person marital status).
    data_tests:
    - accepted_values:
        arguments:
          values:
          - S
          - M
          - D
          - W
          - P
          - N
          - '8'
          - '9'
    config:
      meta:
        fhir_path: Patient.maritalStatus
        fhir_type: code
        value_set: https://fhir.hl7.org.uk/CodeSystem/UKCore-PersonMaritalStatusEngland
  - name: marital_status_display
    data_type: varchar
    description: Human-readable label for marital_status. Not a FHIR element.
  - name: postal_code
    data_type: varchar
    description: Postcode of the patient's usual address (deprivation/geography linkage).
    config:
      meta:
        fhir_path: Patient.address.postalCode
        fhir_type: string
  - name: city
    data_type: varchar
    description: City/town of the patient's usual address.
    config:
      meta:
        fhir_path: Patient.address.city
        fhir_type: string
  - name: lsoa
    data_type: varchar
    description: Lower-layer Super Output Area. Derived from postcode; not a FHIR element.
  - name: identifier
    data_type: variant
    description: Non-NHS-number patient identifiers (Patient.identifier[], excluding nhsNumber).
    config:
      meta:
        fhir_path: Patient.identifier
        fhir_type: Identifier[]
        variant_class: PatientIdentifierVariant
        variant_fields:
        - system
        - value
  - name: meta_source
    data_type: varchar
    description: Source system URI the record originated from.
    data_tests:
    - not_null
    config:
      meta:
        fhir_path: Resource.meta.source
        fhir_type: uri
  - name: meta_tag_code
    data_type: varchar
    description: Feed/extract tag stamped by the source (meta.tag.code).
    config:
      meta:
        fhir_path: Resource.meta.tag.code
        fhir_type: code
  - name: meta_last_updated
    data_type: timestamp_ntz
    description: When the source last changed the record.
    config:
      meta:
        fhir_path: Resource.meta.lastUpdated
        fhir_type: instant
