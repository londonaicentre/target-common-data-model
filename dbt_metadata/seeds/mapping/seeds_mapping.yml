# AUTO-GENERATED by the target-common-data-model (CDM) spec repo - DO NOT EDIT.
# Source of truth: cdm/enums.yaml in that repo. Regenerate there; edits here are overwritten.
seeds:
- name: seed_administrative_gender
  description: Administrative gender (HL7 FHIR administrative-gender, R4 4.0.1).
  config:
    column_types:
      code: varchar
      display: varchar
  columns:
  - name: code
    description: Code value (join key).
  - name: display
    description: Human readable label for the code.
- name: seed_ethnic_category
  description: NHS England ethnic category codes (UKCore-EthnicCategoryEngland).
  config:
    column_types:
      code: varchar
      display: varchar
  columns:
  - name: code
    description: Code value (join key).
  - name: display
    description: Human readable label for the code.
- name: seed_marital_status
  description: NHS England person marital status codes (UKCore-PersonMaritalStatusEngland).
  config:
    column_types:
      code: varchar
      display: varchar
  columns:
  - name: code
    description: Code value (join key).
  - name: display
    description: Human readable label for the code.
- name: seed_encounter_category
  description: CDM-defined encounter category. Primary analyst axis; not a FHIR element.
  config:
    column_types:
      code: varchar
      display: varchar
  columns:
  - name: code
    description: Code value (join key).
  - name: display
    description: Human readable label for the code.
- name: seed_encounter_class
  description: v3 ActEncounterCode - encounter class.
  config:
    column_types:
      code: varchar
      display: varchar
  columns:
  - name: code
    description: Code value (join key).
  - name: display
    description: Human readable label for the code.
- name: seed_admission_method
  description: NHS England admission method codes (UKCore-AdmissionMethodEngland).
  config:
    column_types:
      code: varchar
      display: varchar
  columns:
  - name: code
    description: Code value (join key).
  - name: display
    description: Human readable label for the code.
- name: seed_admit_source
  description: NHS England source of admission codes (UKCore-SourceOfAdmissionEngland).
  config:
    column_types:
      code: varchar
      display: varchar
  columns:
  - name: code
    description: Code value (join key).
  - name: display
    description: Human readable label for the code.
- name: seed_discharge_method
  description: NHS England discharge method codes (UKCore-DischargeMethodEngland).
  config:
    column_types:
      code: varchar
      display: varchar
  columns:
  - name: code
    description: Code value (join key).
  - name: display
    description: Human readable label for the code.
- name: seed_discharge_destination
  description: NHS England discharge destination codes (UKCore-DischargeDestinationEngland).
  config:
    column_types:
      code: varchar
      display: varchar
  columns:
  - name: code
    description: Code value (join key).
  - name: display
    description: Human readable label for the code.
- name: seed_outcome_of_attendance
  description: NHS England outcome of attendance codes (UKCore-OutcomeOfAttendanceEngland).
  config:
    column_types:
      code: varchar
      display: varchar
  columns:
  - name: code
    description: Code value (join key).
  - name: display
    description: Human readable label for the code.
- name: seed_diagnosis_role
  description: Role of the diagnosis for the encounter (HL7 diagnosis-role value set).
  config:
    column_types:
      code: varchar
      display: varchar
  columns:
  - name: code
    description: Code value (join key).
  - name: display
    description: Human readable label for the code.
- name: seed_condition_category
  description: Condition category (UKCore-ConditionCategory + FHIR core condition-category).
  config:
    column_types:
      code: varchar
      display: varchar
  columns:
  - name: code
    description: Code value (join key).
  - name: display
    description: Human readable label for the code.
- name: seed_observation_category
  description: Observation category (HL7 FHIR observation-category).
  config:
    column_types:
      code: varchar
      display: varchar
  columns:
  - name: code
    description: Code value (join key).
  - name: display
    description: Human readable label for the code.
- name: seed_observation_interpretation
  description: Clinical interpretation of an observation result (HL7 FHIR observation-interpretation).
  config:
    column_types:
      code: varchar
      display: varchar
  columns:
  - name: code
    description: Code value (join key).
  - name: display
    description: Human readable label for the code.
