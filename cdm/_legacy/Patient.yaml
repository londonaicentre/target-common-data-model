# AUTO-GENERATED by scripts/generate_linkml.py - DO NOT EDIT.
# Source: https://fhir.hl7.org.uk/StructureDefinition/UKCore-Patient
# Rules: CONVENTIONS.md Part A. Deviations: scripts/config/resources/Patient.yaml
id: https://cdm.aicentre.co.uk/patient
name: patient
description: 'Profile: https://fhir.hl7.org.uk/StructureDefinition/UKCore-Patient (R4 4.0.1). Grain: one
  row per Patient resource.'
annotations:
  grain: one_row_per_resource
  generated_from: https://fhir.hl7.org.uk/StructureDefinition/UKCore-Patient
imports:
- core
classes:
  PatientIdentifierVariant:
    description: Variant for Patient.identifier. NHS number alongside local MRNs; the set of identifiers
      is the information
    attributes:
      assigner_id:
        range: string
        required: false
        description: Organization that issued id (may be just text)
        annotations:
          fhir_path: Patient.identifier.assigner
          fhir_type: Reference(Organization)
      period_end:
        range: TimestampNtz
        required: false
        description: End time with inclusive boundary, if not ongoing
        annotations:
          fhir_path: Patient.identifier.period.end
          fhir_type: dateTime
      period_start:
        range: TimestampNtz
        required: false
        description: Starting time with inclusive boundary
        annotations:
          fhir_path: Patient.identifier.period.start
          fhir_type: dateTime
      system:
        range: uri
        required: false
        description: The namespace for the identifier value
        annotations:
          fhir_path: Patient.identifier.system
          fhir_type: uri
      type:
        range: CodingObject
        required: false
        description: Description of identifier
        annotations:
          fhir_path: Patient.identifier.type
          fhir_type: CodeableConcept
          object_keys:
          - system
          - code
          - display
          value_set: http://hl7.org/fhir/ValueSet/identifier-type
          binding_strength: extensible
          enum: IdentifierTypeEnum
      use:
        range: IdentifierUseEnum
        required: false
        description: usual | official | temp | secondary | old (If known)
        annotations:
          fhir_path: Patient.identifier.use
          fhir_type: code
          value_set: http://hl7.org/fhir/ValueSet/identifier-use|4.0.1
          binding_strength: required
      value:
        range: string
        required: false
        description: The value that is unique
        annotations:
          fhir_path: Patient.identifier.value
          fhir_type: string
  PatientNameVariant:
    description: Variant for Patient.name. official and maiden names are both needed for matching and
      display
    attributes:
      family:
        range: string
        required: false
        description: Family name (often called 'Surname')
        annotations:
          fhir_path: Patient.name.family
          fhir_type: string
      given:
        range: string
        required: false
        description: Given names (not always 'first'). Includes middle names
        annotations:
          fhir_path: Patient.name.given
          fhir_type: string
          flattened_from_repeat: true
      period_end:
        range: TimestampNtz
        required: false
        description: End time with inclusive boundary, if not ongoing
        annotations:
          fhir_path: Patient.name.period.end
          fhir_type: dateTime
      period_start:
        range: TimestampNtz
        required: false
        description: Starting time with inclusive boundary
        annotations:
          fhir_path: Patient.name.period.start
          fhir_type: dateTime
      prefix:
        range: string
        required: false
        description: Parts that come before the name
        annotations:
          fhir_path: Patient.name.prefix
          fhir_type: string
          flattened_from_repeat: true
      suffix:
        range: string
        required: false
        description: Parts that come after the name
        annotations:
          fhir_path: Patient.name.suffix
          fhir_type: string
          flattened_from_repeat: true
      use:
        range: NameUseEnum
        required: false
        description: usual | official | temp | nickname | anonymous | old | maiden
        annotations:
          fhir_path: Patient.name.use
          fhir_type: code
          value_set: http://hl7.org/fhir/ValueSet/name-use|4.0.1
          binding_strength: required
  PatientAddressVariant:
    description: Variant for Patient.address. home/temporary addresses carry distinct analytical meaning
    attributes:
      city:
        range: string
        required: false
        description: Name of city, town etc.
        annotations:
          fhir_path: Patient.address.city
          fhir_type: string
      country:
        range: string
        required: false
        description: Country (e.g. can be ISO 3166 2 or 3 letter code)
        annotations:
          fhir_path: Patient.address.country
          fhir_type: string
      district:
        range: string
        required: false
        description: District name (aka county)
        annotations:
          fhir_path: Patient.address.district
          fhir_type: string
      line:
        range: string
        required: false
        description: Street name, number, direction & P.O. Box etc.
        annotations:
          fhir_path: Patient.address.line
          fhir_type: string
          flattened_from_repeat: true
      period_end:
        range: TimestampNtz
        required: false
        description: End time with inclusive boundary, if not ongoing
        annotations:
          fhir_path: Patient.address.period.end
          fhir_type: dateTime
      period_start:
        range: TimestampNtz
        required: false
        description: Starting time with inclusive boundary
        annotations:
          fhir_path: Patient.address.period.start
          fhir_type: dateTime
      postalcode:
        range: string
        required: false
        description: Postal code for area
        annotations:
          fhir_path: Patient.address.postalCode
          fhir_type: string
      state:
        range: string
        required: false
        description: Sub-unit of country (abbreviations ok)
        annotations:
          fhir_path: Patient.address.state
          fhir_type: string
      type:
        range: AddressTypeEnum
        required: false
        description: postal | physical | both
        annotations:
          fhir_path: Patient.address.type
          fhir_type: code
          value_set: http://hl7.org/fhir/ValueSet/address-type|4.0.1
          binding_strength: required
      use:
        range: AddressUseEnum
        required: false
        description: home | work | temp | old | billing - purpose of this address
        annotations:
          fhir_path: Patient.address.use
          fhir_type: code
          value_set: http://hl7.org/fhir/ValueSet/address-use|4.0.1
          binding_strength: required
  Patient:
    description: 'Profile: https://fhir.hl7.org.uk/StructureDefinition/UKCore-Patient (R4 4.0.1). Grain:
      one row per Patient resource.'
    slots:
    - meta_source
    - meta_tag_code
    - meta_last_updated
    attributes:
      id:
        identifier: true
        range: string
        required: true
        description: Primary key. One row per Patient resource.
        annotations:
          fhir_path: Patient.id
          fhir_type: id
      active:
        range: boolean
        required: false
        description: Whether this patient's record is in active use
        annotations:
          fhir_path: Patient.active
          fhir_type: boolean
      address:
        range: PatientAddressVariant
        multivalued: true
        inlined_as_list: true
        required: false
        description: home/temporary addresses carry distinct analytical meaning
        annotations:
          fhir_path: Patient.address
          fhir_type: Address[]
          variant_key:
          - use
          - postalCode
      birthdate_extension_birthtime_url:
        range: string
        required: true
        description: identifies the meaning of the extension
        annotations:
          fhir_path: Patient.birthDate.extension:birthTime.url
          fhir_type: http://hl7.org/fhirpath/System.String
      birthdate_value:
        range: string
        required: false
        description: Primitive value for date
        annotations:
          fhir_path: Patient.birthDate.value
          fhir_type: http://hl7.org/fhirpath/System.Date
      communication_extension_proficiency_extension_level_url:
        range: uri
        required: true
        description: identifies the meaning of the extension
        annotations:
          fhir_path: Patient.communication.extension:proficiency.extension:level.url
          fhir_type: uri
      communication_extension_proficiency_url:
        range: string
        required: true
        description: identifies the meaning of the extension
        annotations:
          fhir_path: Patient.communication.extension:proficiency.url
          fhir_type: http://hl7.org/fhirpath/System.String
      communication_preferred:
        range: boolean
        required: false
        description: Language preference indicator
        annotations:
          fhir_path: Patient.communication.preferred
          fhir_type: boolean
      deceasedboolean:
        range: boolean
        required: false
        description: Indicates if the individual is deceased or not
        annotations:
          fhir_path: Patient.deceasedBoolean
          fhir_type: boolean
      deceaseddatetime:
        range: TimestampNtz
        required: false
        description: Indicates if the individual is deceased or not
        annotations:
          fhir_path: Patient.deceasedDateTime
          fhir_type: dateTime
      extension_birthsex:
        range: string
        required: false
        description: The patient's phenotypic sex at birth.
        annotations:
          fhir_path: Patient.extension:birthSex
          fhir_type: Extension
      extension_deathnotificationstatus:
        range: string
        required: false
        description: The patient's death notification status.
        annotations:
          fhir_path: Patient.extension:deathNotificationStatus
          fhir_type: Extension
      extension_ethniccategory:
        range: string
        required: false
        description: The ethnicity of the subject
        annotations:
          fhir_path: Patient.extension:ethnicCategory
          fhir_type: Extension
      extension_residentialstatus:
        range: string
        required: false
        description: The residential status of the patient.
        annotations:
          fhir_path: Patient.extension:residentialStatus
          fhir_type: Extension
      gender:
        range: AdministrativeGenderEnum
        required: false
        description: male | female | other | unknown
        annotations:
          fhir_path: Patient.gender
          fhir_type: code
          value_set: http://hl7.org/fhir/ValueSet/administrative-gender|4.0.1
          binding_strength: required
      identifier:
        range: PatientIdentifierVariant
        multivalued: true
        inlined_as_list: true
        required: false
        description: NHS number alongside local MRNs; the set of identifiers is the information
        annotations:
          fhir_path: Patient.identifier
          fhir_type: Identifier[]
          variant_key:
          - system
          - value
      identifier_nhsnumber_assigner_id:
        range: string
        required: false
        description: Organization that issued id (may be just text)
        annotations:
          fhir_path: Patient.identifier:nhsNumber.assigner
          fhir_type: Reference(Organization)
      identifier_nhsnumber_extension_nhsnumberverificationstatus:
        range: string
        required: false
        description: NHS number verification status
        annotations:
          fhir_path: Patient.identifier:nhsNumber.extension:nhsNumberVerificationStatus
          fhir_type: Extension
      identifier_nhsnumber_period_end:
        range: TimestampNtz
        required: false
        description: End time with inclusive boundary, if not ongoing
        annotations:
          fhir_path: Patient.identifier:nhsNumber.period.end
          fhir_type: dateTime
      identifier_nhsnumber_period_start:
        range: TimestampNtz
        required: false
        description: Starting time with inclusive boundary
        annotations:
          fhir_path: Patient.identifier:nhsNumber.period.start
          fhir_type: dateTime
      identifier_nhsnumber_system:
        range: uri
        required: true
        description: The namespace for the identifier value
        annotations:
          fhir_path: Patient.identifier:nhsNumber.system
          fhir_type: uri
      identifier_nhsnumber_type:
        range: CodingObject
        required: false
        description: Description of identifier
        annotations:
          fhir_path: Patient.identifier:nhsNumber.type
          fhir_type: CodeableConcept
          object_keys:
          - system
          - code
          - display
          value_set: http://hl7.org/fhir/ValueSet/identifier-type
          binding_strength: extensible
          enum: IdentifierTypeEnum
      identifier_nhsnumber_use:
        range: IdentifierUseEnum
        required: false
        description: usual | official | temp | secondary | old (If known)
        annotations:
          fhir_path: Patient.identifier:nhsNumber.use
          fhir_type: code
          value_set: http://hl7.org/fhir/ValueSet/identifier-use|4.0.1
          binding_strength: required
      identifier_nhsnumber_value:
        range: string
        required: true
        description: The value that is unique
        annotations:
          fhir_path: Patient.identifier:nhsNumber.value
          fhir_type: string
      maritalstatus:
        range: CodingObject
        required: false
        description: Marital (civil) status of a patient
        annotations:
          fhir_path: Patient.maritalStatus
          fhir_type: CodeableConcept
          object_keys:
          - system
          - code
          - display
          value_set: https://fhir.hl7.org.uk/ValueSet/UKCore-PersonMaritalStatusCode
          binding_strength: extensible
          enum: PersonmaritalstatuscodeEnum
      multiplebirthboolean:
        range: boolean
        required: false
        description: Whether patient is part of a multiple birth
        annotations:
          fhir_path: Patient.multipleBirthBoolean
          fhir_type: boolean
      multiplebirthinteger:
        range: integer
        required: false
        description: Whether patient is part of a multiple birth
        annotations:
          fhir_path: Patient.multipleBirthInteger
          fhir_type: integer
      name:
        range: PatientNameVariant
        multivalued: true
        inlined_as_list: true
        required: false
        description: official and maiden names are both needed for matching and display
        annotations:
          fhir_path: Patient.name
          fhir_type: HumanName[]
          variant_key:
          - use
          - family
