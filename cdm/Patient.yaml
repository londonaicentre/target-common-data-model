# AUTO-GENERATED by scripts/generate_linkml.py - DO NOT EDIT.
#
# Stage 2 of the FlatFHIR generator: the resource whitelist resolved
# against the expanded FHIR element tree, following CONVENTIONS.md.
# Source:  https://fhir.hl7.org.uk/StructureDefinition/UKCore-Patient
# Config:  config/resources/Patient.yaml
#
# Regenerate with:  uv run scripts/generate_linkml.py
id: https://cdm.aicentre.co.uk/patient
name: patient
description: 'Profile: https://fhir.hl7.org.uk/StructureDefinition/UKCore-Patient (FHIR 4.0.1). Grain:
  one row per Patient resource.'
annotations:
  grain: one_row_per_resource
  generated_from: https://fhir.hl7.org.uk/StructureDefinition/UKCore-Patient
  profile_version: 2.5.0
prefixes:
  linkml: https://w3id.org/linkml/
  cdm: https://cdm.aicentre.co.uk/
default_prefix: cdm
imports:
- linkml:types
- enums
default_range: string
classes:
  PatientIdentifier:
    description: An identifier for this patient
    annotations:
      fhir_path: Patient.identifier
      key_fields: system, value
    attributes:
      system:
        range: uri
        description: The namespace for the identifier value
        annotations:
          fhir_path: Patient.identifier.system
          fhir_type: uri
      value:
        range: string
        description: The value that is unique
        annotations:
          fhir_path: Patient.identifier.value
          fhir_type: string
  PatientName:
    description: A name associated with the patient
    annotations:
      fhir_path: Patient.name
      key_fields: use, family
    attributes:
      use:
        range: NameUseEnum
        description: usual | official | temp | nickname | anonymous | old | maiden
        annotations:
          fhir_path: Patient.name.use
          fhir_type: code
          value_set: http://hl7.org/fhir/ValueSet/name-use
          binding_strength: required
          binding_source: fhir
      family:
        range: string
        description: Family name (often called 'Surname')
        annotations:
          fhir_path: Patient.name.family
          fhir_type: string
      given:
        range: string
        multivalued: true
        description: Given names (not always 'first'). Includes middle names
        annotations:
          fhir_path: Patient.name.given
          fhir_type: string
  PatientAddressExtensionAddressKeyExtensionTypeCoding:
    description: Code defined by a terminology system
    annotations:
      fhir_path: Patient.address.extension:addressKey.extension:type.coding
    attributes:
      system:
        range: uri
        description: Identity of the terminology system
        annotations:
          fhir_path: Patient.address.extension:addressKey.extension:type.coding.system
          fhir_type: uri
      code:
        range: UKCoreAddressKeyTypeEnum
        description: Symbol in syntax defined by the system
        annotations:
          fhir_path: Patient.address.extension:addressKey.extension:type.coding.code
          fhir_type: code
          value_set: https://fhir.hl7.org.uk/ValueSet/UKCore-AddressKeyType
          binding_strength: extensible
          binding_source: fhir
      display:
        range: string
        description: Representation defined by the system
        annotations:
          fhir_path: Patient.address.extension:addressKey.extension:type.coding.display
          fhir_type: string
      is_source:
        range: boolean
        description: True on the coding that came from the source system; false on codings added by a
          downstream mapping step. CDM-defined, not native to FHIR (§6).
        annotations:
          fhir_path: Patient.address.extension:addressKey.extension:type.coding.is_source
          fhir_type: boolean
  PatientAddressExtensionAddressKeyExtensionType:
    description: Address Key Type
    annotations:
      fhir_path: Patient.address.extension:addressKey.extension:type
    attributes:
      coding:
        range: PatientAddressExtensionAddressKeyExtensionTypeCoding
        multivalued: true
        inlined_as_list: true
        description: Code defined by a terminology system
        annotations:
          fhir_path: Patient.address.extension:addressKey.extension:type.coding
          fhir_type: Coding
      text:
        range: string
        description: Plain text representation of the concept
        annotations:
          fhir_path: Patient.address.extension:addressKey.extension:type.text
          fhir_type: string
  PatientAddressExtensionAddressKey:
    description: A patient's address key and type
    annotations:
      fhir_path: Patient.address.extension:addressKey
    attributes:
      extension_type:
        range: PatientAddressExtensionAddressKeyExtensionType
        required: true
        description: Address Key Type
        annotations:
          fhir_path: Patient.address.extension:addressKey.extension:type
          fhir_type: CodeableConcept
      extension_value:
        range: string
        required: true
        description: Address key value
        annotations:
          fhir_path: Patient.address.extension:addressKey.extension:value
          fhir_type: string
  PatientAddressPeriod:
    description: Time period when address was/is in use
    annotations:
      fhir_path: Patient.address.period
    attributes:
      start:
        range: datetime
        description: Starting time with inclusive boundary
        annotations:
          fhir_path: Patient.address.period.start
          fhir_type: dateTime
      end:
        range: datetime
        description: End time with inclusive boundary, if not ongoing
        annotations:
          fhir_path: Patient.address.period.end
          fhir_type: dateTime
  PatientAddress:
    description: An address for the individual
    annotations:
      fhir_path: Patient.address
      key_fields: period, postalcode
    attributes:
      extension_addresskey:
        range: PatientAddressExtensionAddressKey
        multivalued: true
        inlined_as_list: true
        description: A patient's address key and type
        annotations:
          fhir_path: Patient.address.extension:addressKey
          fhir_type: Extension
      use:
        range: AddressUseEnum
        description: home | work | temp | old | billing - purpose of this address
        annotations:
          fhir_path: Patient.address.use
          fhir_type: code
          value_set: http://hl7.org/fhir/ValueSet/address-use
          binding_strength: required
          binding_source: fhir
      line:
        range: string
        multivalued: true
        description: Street name, number, direction & P.O. Box etc.
        annotations:
          fhir_path: Patient.address.line
          fhir_type: string
      city:
        range: string
        description: Name of city, town etc.
        annotations:
          fhir_path: Patient.address.city
          fhir_type: string
      postalcode:
        range: string
        description: Postal code for area
        annotations:
          fhir_path: Patient.address.postalCode
          fhir_type: string
      country:
        range: string
        description: Country (e.g. can be ISO 3166 2 or 3 letter code)
        annotations:
          fhir_path: Patient.address.country
          fhir_type: string
      period:
        range: PatientAddressPeriod
        description: Time period when address was/is in use
        annotations:
          fhir_path: Patient.address.period
          fhir_type: Period
  PatientExtensionEthnicCategoryCoding:
    description: Code defined by a terminology system
    annotations:
      fhir_path: Patient.extension:ethnicCategory.coding
    attributes:
      system:
        range: uri
        description: Identity of the terminology system
        annotations:
          fhir_path: Patient.extension:ethnicCategory.coding.system
          fhir_type: uri
      code:
        range: EthnicCategoryEnum
        description: Symbol in syntax defined by the system
        annotations:
          fhir_path: Patient.extension:ethnicCategory.coding.code
          fhir_type: code
          value_set: https://fhir.hl7.org.uk/ValueSet/UKCore-EthnicCategory
          binding_strength: extensible
          binding_source: manual
      display:
        range: string
        description: Representation defined by the system
        annotations:
          fhir_path: Patient.extension:ethnicCategory.coding.display
          fhir_type: string
      is_source:
        range: boolean
        description: True on the coding that came from the source system; false on codings added by a
          downstream mapping step. CDM-defined, not native to FHIR (§6).
        annotations:
          fhir_path: Patient.extension:ethnicCategory.coding.is_source
          fhir_type: boolean
  PatientExtensionEthnicCategory:
    description: A code classifying the person's ethnicity
    annotations:
      fhir_path: Patient.extension:ethnicCategory
    attributes:
      coding:
        range: PatientExtensionEthnicCategoryCoding
        multivalued: true
        inlined_as_list: true
        description: Code defined by a terminology system
        annotations:
          fhir_path: Patient.extension:ethnicCategory.coding
          fhir_type: Coding
  Patient:
    description: One row per Patient resource.
    annotations:
      fhir_path: Patient
    attributes:
      id:
        range: string
        identifier: true
        description: Logical id of this resource.
        annotations:
          fhir_path: Patient.id
          fhir_type: id
      meta_source:
        range: uri
        description: Identifies where the resource comes from.
        annotations:
          fhir_path: Patient.meta.source
          fhir_type: uri
      meta_last_updated:
        range: datetime
        description: When the resource version last changed.
        annotations:
          fhir_path: Patient.meta.lastUpdated
          fhir_type: instant
      identifier_nhsnumber_value:
        range: string
        required: true
        description: The value that is unique
        annotations:
          fhir_path: Patient.identifier:nhsNumber.value
          fhir_type: string
      identifier:
        range: PatientIdentifier
        multivalued: true
        inlined_as_list: true
        description: An identifier for this patient
        annotations:
          fhir_path: Patient.identifier
          fhir_type: Identifier
      name_family:
        range: string
        description: Family name (often called 'Surname')
        annotations:
          fhir_path: Patient.name.family
          fhir_type: string
      name_given:
        range: string
        description: Given names (not always 'first'). Includes middle names
        annotations:
          fhir_path: Patient.name.given
          fhir_type: string
      name:
        range: PatientName
        multivalued: true
        inlined_as_list: true
        description: A name associated with the patient
        annotations:
          fhir_path: Patient.name
          fhir_type: HumanName
      gender:
        range: AdministrativeGenderEnum
        description: male | female | other | unknown
        annotations:
          fhir_path: Patient.gender
          fhir_type: code
          value_set: http://hl7.org/fhir/ValueSet/administrative-gender
          binding_strength: required
          binding_source: fhir
      birthdate:
        range: date
        description: The date of birth for the individual
        annotations:
          fhir_path: Patient.birthDate
          fhir_type: date
      deceasedboolean:
        range: boolean
        description: Indicates if the individual is deceased or not
        annotations:
          fhir_path: Patient.deceasedBoolean
          fhir_type: boolean
      deceaseddatetime:
        range: datetime
        description: Indicates if the individual is deceased or not
        annotations:
          fhir_path: Patient.deceasedDateTime
          fhir_type: dateTime
      address_postalcode:
        range: string
        description: Postal code for area
        annotations:
          fhir_path: Patient.address.postalCode
          fhir_type: string
      address:
        range: PatientAddress
        multivalued: true
        inlined_as_list: true
        description: An address for the individual
        annotations:
          fhir_path: Patient.address
          fhir_type: Address
      extension_ethniccategory:
        range: PatientExtensionEthnicCategory
        required: true
        description: A code classifying the person's ethnicity
        annotations:
          fhir_path: Patient.extension:ethnicCategory
          fhir_type: CodeableConcept
